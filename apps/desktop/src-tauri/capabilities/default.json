{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum permissions for the main analyzer window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}

