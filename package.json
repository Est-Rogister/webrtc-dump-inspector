{
  "name": "webrtc-analyzer-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run tauri:dev --workspace=@webrtc-analyzer/desktop",
    "build": "npm run build --workspace=@webrtc-analyzer/desktop",
    "check": "npm run check --workspace=@webrtc-analyzer/desktop",
    "test": "npm run test --workspace=@webrtc-analyzer/desktop",
    "tauri:build": "npm run tauri:build --workspace=@webrtc-analyzer/desktop"
  }
}

